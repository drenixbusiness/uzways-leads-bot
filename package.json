{
  "name": "leads-daily-bot",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "start": "node index.js",
    "report": "node index.js --report",
    "report:send": "node index.js --report --send",
    "test": "node --test",
    "report:yesterday": "node index.js --report --yesterday",
    "check:sheet": "node debug-fetch.mjs",
    "pm2:start": "npx pm2 start ecosystem.config.js",
    "pm2:stop": "npx pm2 stop ecosystem.config.js",
    "pm2:restart": "npx pm2 restart ecosystem.config.js",
    "pm2:save": "npx pm2 save",
    "deploy:linux": "bash deploy.sh",
    "deploy:windows": "powershell -ExecutionPolicy Bypass -File deploy.ps1",
    "pm2:install-service": "npx pm2-service-install --startup-start-mode auto"
  },
  "dependencies": {
    "dotenv": "^16.4.7",
    "googleapis": "^160.0.0",
    "luxon": "^3.5.0",
    "node-cron": "^3.0.3"
  },
  "devDependencies": {
    "pm2": "^5.3.0"
  }
}
